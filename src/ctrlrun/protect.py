"""The @protect decorator and Control.execute. Build-list item 3; SPEC-v0.1 §8."""

# SPEC: §2.2 — Control.execute must invoke the executor with Action.canonical_arguments,
# never the caller's original argument objects.
